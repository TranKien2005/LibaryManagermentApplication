{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "maven-build",
            "type": "shell",
            "command": "mvn clean install",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}