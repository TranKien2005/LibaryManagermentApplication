server:
  port: ${BACKEND_PORT}
  servlet:
    context-path: /api

spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: my-consumer-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      max-poll-records: 10      # giảm số message mỗi lần poll
      fetch-max-bytes: 1048576
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  mail:
    host: smtp.gmail.com
    port: 587
    username: minhquan15032005@gmail.com
    password: ${MAIL_PASSWORD}
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  redis:
    host: ${SPRING_REDIS_HOST}
    port: ${SPRING_REDIS_PORT}
throttling:
  limit: 50
  duration: 10
  base-delay: 200
jwt:
  signerKey: ${JWT_KEY}
  accessToken:
    expirationTime: 1800
  refreshToken:
    expirationTime: 604800
# redisson:
#   singleServerConfig:
#     address: "redis://${SPRING_REDIS_HOST}:${SPRING_REDIS_PORT}"
#     connectionMinimumIdleSize: 2
#     connectionPoolSize: 10


app:
 cors:
   allowed-origins: "*"


#app:
# cors:
#   allowed-origins: "*"
#server:
# port: 8080
# servlet:
#   context-path: /api
#
#spring:
# datasource:
#   url: jdbc:mysql://localhost:3306/librarymanagement
#   username: root
#   password: 123456
# jpa:
#   hibernate:
#     ddl-auto: update
#   show-sql: true
#   properties:
#     hibernate:
#       format_sql: true
#jwt:
#  # NOTE: HS512 requires a strong secret (recommended >= 64 bytes).
#  # This value is a 128-hex-char string (64 bytes) used for HMAC signing.
#  signerKey: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
#  accessToken:
#    expirationTime: 1800
#  refreshToken:
#    expirationTime: 604800

